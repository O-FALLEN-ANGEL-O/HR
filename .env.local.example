# Supabase credentials for client-side and server-side access.
# These should be added to your Vercel project's environment variables.
SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# These are the public versions needed for middleware and client components.
# They will have the same values as the ones above.
NEXT_PUBLIC_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# This is the service role key, used for administrative tasks like seeding the database.
# It should ONLY be used on the server and should be kept secret.
# Add this to Vercel as well so the database can be seeded on build.
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"
